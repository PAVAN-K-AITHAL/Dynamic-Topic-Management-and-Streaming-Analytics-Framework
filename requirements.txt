# ============================================================
# Fraud Detection Pipeline — Root Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# ── Core Pipeline (Kafka, Admin, Config) ─────────────────────
kafka-python-ng              # Maintained fork of kafka-python (drop-in replacement)
flask>=3.0
flask-cors>=6.0
sqlalchemy>=2.0
requests>=2.31
streamlit>=1.40
redis>=5.0
python-dotenv>=1.0

# ── ML / AI ──────────────────────────────────────────────────
scikit-learn>=1.5
xgboost>=2.0
torch                        # Already installed (CPU)
shap>=0.44
optuna>=4.0
numpy>=2.0
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
imbalanced-learn>=0.12       # SMOTE oversampling
joblib>=1.4

# ── MLOps ────────────────────────────────────────────────────
mlflow>=2.15
evidently>=0.5
prometheus-client>=0.20
prometheus-fastapi-instrumentator>=7.0

# ── Stream Processing ───────────────────────────────────────
pyspark==3.5.6

# ── Model Serving ───────────────────────────────────────────
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0

# ── Testing ─────────────────────────────────────────────────
pytest>=8.0
locust>=2.30

# ── Notebooks ───────────────────────────────────────────────
jupyter>=1.0
notebook>=7.0
